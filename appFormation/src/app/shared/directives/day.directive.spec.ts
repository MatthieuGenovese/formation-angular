import { DayDirective } from './day.directive';

describe('DayDirective', () => {
  it('should create an instance', () => {
    const directive = new DayDirective();
    expect(directive).toBeTruthy();
  });
});
